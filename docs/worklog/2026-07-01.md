# 2026-07-01 작업 일지

 1) 작업 전 상태 확인:완료
   (1) Git 상태 확인:완료
       - 작업 전 미커밋 변경 파일은 확인되지 않았다.
       - 작업 전 diff 통계는 비어 있었다.
   (2) 참고 자료 접근 범위 확인:확인
       - port-view/.kiro 하위 최신 문서는 참고 자료로만 사용하고 Preprocessor 책임 범위 사실만 선별했다.
       - 실제 cluster 이름, task definition ARN, image URI, subnet, security group, command id, secret ARN 원문은 이 저장소 문서에 옮기지 않았다.

 2) Preprocessor 책임 경계 정리:완료
   (1) Preprocessor 담당 역할 정리:완료
       - `interest` schema raw/history 데이터를 읽어 `preprocessor` schema feature 테이블을 생성/갱신하는 책임을 README에 정리했다.
       - 뉴스, 증권사 리포트, 가격, 시장 폭, 매크로, 원자재, 해외지수, 투자자 수급, 프로그램 매매, 공매도, total stock/market feature 생성 책임을 정리했다.
   (2) Preprocessor 직접 책임 아닌 영역 정리:완료
       - 외부 데이터 수집(Crawler), 매수/매도 판단·주문 생성·주문 제출·체결 조회·잔고 갱신(StrategyDecision·StrategyExecution·MarketConnector), 백테스트(StrategyResearch), 화면 렌더링/승인 UI(View), Daily Batch 전체 orchestration(Scheduler·Step Functions)은 Preprocessor 책임이 아님을 README에 명시했다.

 3) 데이터 흐름 관점 정리:완료
   (1) README 데이터 흐름 섹션 추가:완료
       - Crawler → `interest` schema raw → Preprocessor `preprocessor` schema feature → StrategyDecision/StrategyResearch 후속 소비 관계를 Preprocessor 관점에서만 서술했다.
       - 인접 MS 내부 구현 상세는 반영하지 않았다.

 4) AWS 운영 구조 관점 문서화:완료
   (1) README AWS 운영 섹션 추가:완료
       - AWS Paper Daily Step 3에서 Preprocessor가 전처리 실행 단위로 호출될 수 있음을 서술했다.
       - ECS RunTask 또는 컨테이너 실행 대상으로 `pre_daily.py` 또는 개별 `pre_*.py`가 실행될 수 있음을 서술했다.
       - 실제 cluster 이름, task definition ARN, image URI, subnet, security group, command id는 원문으로 기록하지 않고 `<PREPROCESSOR_ECS_TASK>` 등 마스킹 표기만 사용한다는 원칙을 반영했다.
   (2) Daily Step 3 전처리 흐름 정리:완료
       - `pre_daily.py`의 현재 소스 기준 호출 순서를 정리했다.
       - `pre_agency_analysis.run()`이 현재 주석 처리되어 실행되지 않는 상태를 정적 확인으로 명시했다.

 5) source-file-catalog 최신화:완료
   (1) `docs/source-file-catalog.md` 갱신:완료
       - 실행 위험 요약 섹션을 추가해 DB read/upsert 가능 파일, 외부 holiday API 호출 가능 파일, 대량 feature 갱신 가능 파일을 분류했다.
   (2) 개별 파일 주의사항 반영:완료
       - `pre_daily.py`와 `pre_agency_analysis.py` 항목에 `pre_agency_analysis.run()` 호출이 주석 처리된 정적 확인 결과를 반영했다.
       - `pre_daily.py` 주의사항에 AWS Paper Daily Step 3에서 ECS RunTask 또는 컨테이너 실행 대상으로 호출될 수 있다는 Preprocessor 관점 설명을 추가했다.

 6) CHANGELOG 최신화:완료
   (1) `## 2026-07-01` 섹션 추가:완료
       - Added 항목에 README AWS 운영 섹션, 책임 경계 섹션, 데이터 흐름 섹션, Daily Step 3 흐름 섹션, 카탈로그 실행 위험 요약, worklog 신규 작성 내용을 기록했다.
       - Changed 항목에 카탈로그의 `pre_daily.py`, `pre_agency_analysis.py` 항목 반영 내용을 기록했다.
       - Notes 항목에 문서 전용 변경 원칙, 다른 MS 내부 상세와 세부 운영 로그 반영 제외, 실제 실행 미수행, 민감정보/운영 식별자 원문 미기록 원칙을 명시했다.

 7) 검증:완료
   (1) 문서 전용 검증:완료
       - `git status --short`, `git diff --stat`으로 md 문서만 변경됨을 확인할 예정이다.
       - 실제 전처리 실행, 외부 API 호출, 모델 다운로드/학습/추론, DB DDL/DML, 크롤링, 주문 실행, backfill, validation job은 수행하지 않았다.

 8) 민감정보 취급:확인
   (1) 문서 기록 금지 항목 준수:확인
       - DB password, API key, token, Slack webhook URL, 계좌번호, cluster/task definition ARN, image URI, subnet, security group, secret ARN, IAM Role ARN, command id, DB connection string 원문은 문서에 기록하지 않았다.
   (2) placeholder 사용 규칙:확인
       - 값 마스킹이 필요한 위치에는 `[REDACTED]`, `<PREPROCESSOR_ECS_TASK>`, `<TASK_DEFINITION_ARN>`, `<ECR_IMAGE_URI>`, `<COMMAND_ID>`, `<DB_HOST>` placeholder만 사용했다.

 9) 남은 위험 및 후속 작업:예정
   (1) `pre_agency_analysis` 재활성화 여부:예정
       - 현재 `pre_daily.py`에서 주석 처리된 호출을 재활성화할지는 별도 검토 후 결정한다.
